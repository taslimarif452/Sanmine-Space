import { createCipheriv, createDecipheriv, createHmac, randomBytes, timingSafeEqual } from "node:crypto";

const GOOGLE_AUTH_URL="https://accounts.google.com/o/oauth2/v2/auth";const GOOGLE_TOKEN_URL="https://oauth2.googleapis.com/token";const GOOGLE_USERINFO_URL="https://www.googleapis.com/oauth2/v2/userinfo";const MICROSOFT_AUTH_URL="https://login.microsoftonline.com";const MICROSOFT_GRAPH_URL="https://graph.microsoft.com/v1.0/me";export type EmailProvider="google"|"microsoft";
function env(name:string){return process.env[name]?.trim()||""}function stateSecret(){const value=env("OAUTH_STATE_SECRET")||env("OAUTH_TOKEN_ENCRYPTION_KEY");if(!value)throw new Error("OAUTH_STATE_SECRET is not configured.");return value}function signState(value:string){return createHmac("sha256",stateSecret()).update(value).digest("base64url")}export function createOAuthState(uid:string){const payload=Buffer.from(JSON.stringify({uid,exp:Date.now()+10*60*1000,nonce:randomBytes(16).toString("hex")})).toString("base64url");return`${payload}.${signState(payload)}`}export function verifyOAuthState(state:string){const[payload,signature]=state.split(".");if(!payload||!signature)throw new Error("Invalid OAuth state.");const expected=signState(payload),a=Buffer.from(signature),b=Buffer.from(expected);if(a.length!==b.length||!timingSafeEqual(a,b))throw new Error("Invalid OAuth state signature.");const data=JSON.parse(Buffer.from(payload,"base64url").toString("utf8")) as{uid?:string;exp?:number};if(!data.uid||!data.exp||data.exp<Date.now())throw new Error("OAuth state expired.");return data}
export function encryptionKey(){const value=env("OAUTH_TOKEN_ENCRYPTION_KEY");if(!value)throw new Error("OAUTH_TOKEN_ENCRYPTION_KEY is not configured.");return createHmac("sha256",value).update("sanmine-email-tokens").digest()}export function encryptToken(value:string){const iv=randomBytes(12),cipher=createCipheriv("aes-256-gcm",encryptionKey(),iv),ciphertext=Buffer.concat([cipher.update(value,"utf8"),cipher.final()]);return`${iv.toString("base64url")}.${cipher.getAuthTag().toString("base64url")}.${ciphertext.toString("base64url")}`}export function decryptToken(value:string){const[ivRaw,tagRaw,ciphertextRaw]=value.split(".");if(!ivRaw||!tagRaw||!ciphertextRaw)throw new Error("Invalid encrypted token.");const decipher=createDecipheriv("aes-256-gcm",encryptionKey(),Buffer.from(ivRaw,"base64url"));decipher.setAuthTag(Buffer.from(tagRaw,"base64url"));return Buffer.concat([decipher.update(Buffer.from(ciphertextRaw,"base64url")),decipher.final()]).toString("utf8")}
export function providerConfig(provider:EmailProvider,origin:string){if(provider==="google"){const clientId=env("GOOGLE_OAUTH_CLIENT_ID"),clientSecret=env("GOOGLE_OAUTH_CLIENT_SECRET"),redirectUri=env("GOOGLE_OAUTH_REDIRECT_URI")||`${origin}/api/email/google/callback`;if(!clientId||!clientSecret)throw new Error("Google email OAuth is not configured.");return{clientId,clientSecret,redirectUri}}const clientId=env("MICROSOFT_CLIENT_ID"),clientSecret=env("MICROSOFT_CLIENT_SECRET"),tenant=env("MICROSOFT_TENANT_ID")||"common",redirectUri=env("MICROSOFT_REDIRECT_URI")||`${origin}/api/email/microsoft/callback`;if(!clientId||!clientSecret)throw new Error("Microsoft email OAuth is not configured.");return{clientId,clientSecret,tenant,redirectUri}}
export function googleAuthorizationUrl(state:string,origin:string){const{clientId,redirectUri}=providerConfig("google",origin);const params=new URLSearchParams({client_id:clientId,redirect_uri:redirectUri,response_type:"code",access_type:"offline",prompt:"consent",scope:"openid email profile https://www.googleapis.com/auth/gmail.send https://www.googleapis.com/auth/gmail.modify",state});return`${GOOGLE_AUTH_URL}?${params}`}export function microsoftAuthorizationUrl(state:string,origin:string){const{clientId,tenant,redirectUri}=providerConfig("microsoft",origin);const params=new URLSearchParams({client_id:clientId,redirect_uri:redirectUri,response_type:"code",response_mode:"query",scope:"openid profile email offline_access User.Read Mail.Send",state});return`${MICROSOFT_AUTH_URL}/${tenant}/oauth2/v2.0/authorize?${params}`}
export async function exchangeGoogleCode(code:string,origin:string){const{clientId,clientSecret,redirectUri}=providerConfig("google",origin);const response=await fetch(GOOGLE_TOKEN_URL,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:redirectUri,grant_type:"authorization_code"})});const data=await response.json();if(!response.ok||!data.access_token)throw new Error(String(data.error_description||data.error||"Google OAuth token exchange failed."));const profileResponse=await fetch(GOOGLE_USERINFO_URL,{headers:{Authorization:`Bearer ${data.access_token}`}}),profile=await profileResponse.json();if(!profileResponse.ok||!profile.email)throw new Error("Unable to read the Google account email.");return{email:String(profile.email),name:profile.name?String(profile.name):null,accessToken:String(data.access_token),refreshToken:data.refresh_token?String(data.refresh_token):null,expiresAt:Date.now()+Number(data.expires_in||3600)*1000}}
export async function exchangeMicrosoftCode(code:string,origin:string){const{clientId,clientSecret,tenant,redirectUri}=providerConfig("microsoft",origin);const response=await fetch(`${MICROSOFT_AUTH_URL}/${tenant}/oauth2/v2.0/token`,{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:clientId,client_secret:clientSecret,redirect_uri:redirectUri,grant_type:"authorization_code",scope:"openid profile email offline_access User.Read Mail.Send"})});const data=await response.json();if(!response.ok||!data.access_token)throw new Error(String(data.error_description||data.error||"Microsoft OAuth token exchange failed."));const profileResponse=await fetch(MICROSOFT_GRAPH_URL,{headers:{Authorization:`Bearer ${data.access_token}`}}),profile=await profileResponse.json();if(!profileResponse.ok)throw new Error("Unable to read the Microsoft account profile.");const email=profile.mail||profile.userPrincipalName;if(!email)throw new Error("Unable to determine the Microsoft account email.");return{email:String(email),name:profile.displayName?String(profile.displayName):null,accessToken:String(data.access_token),refreshToken:data.refresh_token?String(data.refresh_token):null,expiresAt:Date.now()+Number(data.expires_in||3600)*1000}}
