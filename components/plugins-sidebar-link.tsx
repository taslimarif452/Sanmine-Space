"use client";

// Kept as a compatibility export for existing imports. Navigation now lives
// exclusively in WorkspaceSidebar so every workspace page uses one sidebar.
export function PluginsSidebarLink(){return null;}
